import { Container, Typography } from '@mui/material';

const NeedAction = () => {
  return (
    <Container maxWidth="md" className="mt-12">
      <Typography variant="h4" gutterBottom>
        Need Action
      </Typography>
      <Typography variant="body1">This is the Need Action page.</Typography>
    </Container>
  );
};

export default NeedAction;
